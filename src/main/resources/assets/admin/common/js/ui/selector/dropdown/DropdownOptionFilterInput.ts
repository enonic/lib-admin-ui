import {OptionFilterInput} from '../OptionFilterInput';

export class DropdownOptionFilterInput
    extends OptionFilterInput {

    constructor(placeholderText?: string) {
        super(placeholderText);
    }
}


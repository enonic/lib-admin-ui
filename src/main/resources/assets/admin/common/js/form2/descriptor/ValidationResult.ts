// Re-exported from @enonic/input-types: the input types live in the toolkit now (#4692).

export type {ResolvePhrase, ValidationMessage, ValidationResult} from '@enonic/input-types';
export {invalidValue, resolveValidationMessage} from '@enonic/input-types';
